# 2026-07-28

- `[chat:rsv]` Zmieniono publikacyjny protokół na dwa kanałowe punkty post hoc: `stage3_avgpool` po `main_branch.0` oraz `stage4_avgpool` po natywnym `avgpool`; oba są zbierane w tych samych forwardach dla checkpointów faz.
- `[chat:bootstrap]` Dodano klasowo stratyfikowany hierarchiczny bootstrap sparowanych różnic po modelach/seedach i obrazach, z walidacją indeksów i protokołu.
- `[chat:batchnorm]` Dodano domyślnie wyłączoną rekalibrację buforów BN wspólnego trzonu przed fazą 4; operacja nie zmienia wag ani RNG i jest pomijana przy resume wewnątrz fazy.
- `[chat:validation]` Targeted CPU: `28 passed`, po poprawkach brzegowych `22 passed`; pełna CPU: `48 passed, 11 deselected`. GH200 Slurm `20022274`: `11 passed`, `COMPLETED`, `0:0`, `MaxRSS=3825024K`; obejmuje dwa punkty RSV i rekalibrację BN pełnego ResNetu.
- `[chat:layout]` Przeniesiono sześć archiwalnych notebooków do `notebooks/` i dwa artykuły PDF do `docs/papers/`; zaktualizowano odwołania dokumentacyjne.
- `[chat:smoke]` Wysłano GH200 Slurm `20023482`: S-ResNet-18/CIFAR-10, cztery fazy po jednej epoce, batch 512, FIM 0,2% z `M=1`, W&B disabled; na życzenie użytkownika bez aktywnego monitorowania.
- `[chat:runstats-resume]` Dodano fazowe referencje `RunStatsBiModal` dla lewej, prawej i wspólnej gałęzi oraz całego modelu. Checkpoint v3 zachowuje referencje i akumulatory; legacy model-only `resume_checkpoint` jest odrzucany, a jawny weights-only resume wymaga `resume_start_epoch`.
- `[chat:runstats-validation]` Targeted CPU: 37 testów, pełna CPU: `50 passed, 11 deselected`; GH200 `20025645`: `11 passed`, `COMPLETED`, `0:0`, `MaxRSS=3233344K`; `compileall` i `git diff --check` zakończone powodzeniem.

- `[chat:loop-audit-3-5]` UMT loguje całkowitą optymalizowaną stratę i jej składowe; fazy 3/4 jawnie wymuszają prawą transformację `proper`; checkpoint v4 zapisuje i waliduje manifest protokołu przy pełnym resume. Targeted CPU: `22 passed`; pełna CPU: `50 passed, 11 skipped`; GH200 Slurm `20026328`: `11 passed` w 64,51 s, `COMPLETED`, `0:0`, `MaxRSS=2953408K`.

- `[chat:loop-audit-7-8]` LR jest logowany bez agregacji po próbkach; manifest protokołu v2 używa jawnego `fim_measurements_per_epoch=2` i dokładnych lokalnych pozycji zamiast globalnego modulo. Scalono seedowanie trenera ze wspólną implementacją. Targeted CPU: `34 passed`; pełna CPU: `53 passed, 11 skipped`; GH200 Slurm `20027033`: `11 passed` w 54,04 s, `COMPLETED`, `0:0`, `MaxRSS=3957824K`.

- `[chat:loop-audit-6]` Długość trajektorii nie używa już `lr × ||gradient||`; po każdym kroku optimizera akumuluje rzeczywiste przesunięcia parametrów, także z weight decay i momentum. Stan ostatniego kroku jest przy resume rekonstruowany z modelu bez duplikowania wag w checkpointcie. Targeted CPU: `28 passed`; pełna CPU: `55 passed, 11 skipped`; Pierwszy GH200 `20027576`: `11 passed`; finalny stan bez duplikatu wag w checkpoincie, GH200 `20027672`: `11 passed` w 51,01 s, `COMPLETED`, `0:0`, `MaxRSS=3858752K`.


- `[chat:smoke-final]` Zastąpiono oczekujący job `20023482` finalnym czterofazowym smoke runem GH200 `20027957` (`qos=now`): S-ResNet-18/CIFAR-10, po jednej epoce na fazę, batch 512, seed 83, FIM 0,2% z `M=1` i dwoma pomiarami na epokę, W&B disabled. Job uruchomił się na `x1002c7s4b0n0`; wyniki: `/net/storage/pr3/plgrid/plggdnnp/reports/clpintervention_smoke`, log Slurm: `slurm_logs/experiment-20027957.out`.

- `[chat:smoke-rerun]` Po poprawie tensorów zerowego gradientu na urządzeniu parametru i teście CPU (`20 passed`) gate GPU `20028218` zakończył się `12 passed` w 63,24 s (`COMPLETED`, `0:0`, MaxRSS `4095040K`). Czterofazowy smoke run GH200 `20028303` również zakończył się poprawnie (`COMPLETED`, `0:0`, 1 min 52 s, MaxRSS `5020096K`) i zapisał checkpointy epok 0–4.

- `[chat:manifest-freeze]` Dodano atomowy `run_manifest.json` z treściowymi hashami kodu, datasetu, wejściowego checkpointu i artefaktów, pełną konfiguracją, środowiskiem oraz Job ID. Zamrożono baseline CIFAR-10/S-ResNet-18 seed 83 (`80/200/96/200`, LR 0,6, FIM 2% `M=5`, native BN); naukowe nadpisania CLI są odrzucane. Targeted CPU: `20 passed`; pełna CPU: `61 passed, 12 skipped`; manifestowy smoke GH200 `20028930`: `COMPLETED`, `0:0`, 1 min 58 s, MaxRSS `5487936K`, pięć checkpointów zweryfikowanych SHA-256.

- `[chat:freeze-fim]` Audyt fazy 3 potwierdził odłączenie lewej gałęzi od forwardu; dodano pełnomodelowy test braku gradientów, zmian parametrów, momentum i buforów BN. Sampled FIM porównano z naiwną pętlą per-example oraz dodano TFIM na parametr gałęzi i osobno na parametr wagowy. Targeted CPU: `17 passed`; GPU gate `20042968`: `13 passed` w 57,66 s, `COMPLETED`, `0:0`, MaxRSS `4121472K`.

- `[chat:validation-protocol]` Dodano stały split CIFAR-10 44k/5k/1k seed 83, manifest hashy, domenowe statystyki train 44k (compute job `20047159`), jawne generatory loaderów i final-only test loadery. Zabroniono wszystkich batchowych operacji tensorowych i iterowania loaderów na login nodzie w `AGENTS.md`.
- `[chat:validation-control]` Dodano wspólną ewaluację modalności, Phase 2 plateau/rolling selection, Phase 3 recovery stopper/compatibility drift/fallbacki, dwa selektory Phase 4, checkpoint v5 z controller/loader state oraz świeży optimizer/scheduler na granicy faz.
- `[chat:fim-chunking]` Stały FIM probe 1k przetwarzany jest w deterministycznych chunkach z agregacją przez `M*N`; dodano `fim_chunk_size=16` i regresję równoważności z niechunkowanym wynikiem.
- `[chat:validation-smoke]` GH200 `20048588` zakończył się `COMPLETED`, ale ujawnił brak przekazania płaskich długości faz do nowego trenera; wykonał tylko Phase 4 epoch 0 i nie jest zaliczany jako czterofazowy smoke. Błąd poprawiono; rerun `20049057` wysłany. Hashy wszystkich siedmiu plików CIFAR-10 dostarczył compute job `20048952` (`COMPLETED`).
- `[chat:validation-gates]` Finalny smoke z retencją granic faz `20049968`: `COMPLETED`, `0:0`, 1:17; zachował Phase 1/2/3 boundary, pre-Phase-3, wybrane kandydaty, Phase 4 i `resume_current`, bez ostrzeżeń o niemonotonicznym kroku W&B. Gate realnych loaderów + pełnych modeli GPU `20049623`: `15 passed`; pełny gate nie-GPU na compute po poprawie mocków `20050267`: `91 passed, 13 deselected`.
- `[chat:validation-final]` Wszystkie operacje wykonano na compute node. Łączny gate `20051582`: `106 passed`, 3 ostrzeżenia zgodności torchvision z NumPy 2.4, `COMPLETED`, `0:0`, 1:42. Resume smoke `20051596`: `COMPLETED`, `0:0`, 1:36; wznowił od Phase 2 boundary, pominął Phase 1, zachował source step, wykonał Phase 3/4 i final-only test. Pełnodatasetowy pilot seed 83 `20051568` wystartował w `observe_only` z W&B offline; wynik pozostaje oczekujący.
