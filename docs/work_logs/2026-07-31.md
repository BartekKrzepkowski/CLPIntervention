# 2026-07-31

- `[chat:results]` Gold P1=0/P2=200 osiągnął test proper accuracy 89,82%,
  88,47% i 88,50% (średnia 88,93%) dla seedów 83/184/285.
- `[chat:stopper-audit]` P1=40 shadow miał 0/120 checkpointów safe/feasible;
  wszystkie trzy P4 wystartowały po rollbacku z `e3=0`. Weak-only loss miał
  minimum w epokach 15/10/20 mimo późniejszego wzrostu accuracy.
- `[chat:accuracy-first]` Weak-recovery v3 opiera feasible, plateau i reversal
  przede wszystkim na sparowanej weak-only accuracy. Loss i compatibility drift
  pozostają diagnostyką oraz tie-breakerami.
- `[chat:warmup]` Dodano opcjonalny, checkpointowalny liniowy warm-up LR
  Phase 3. Profil P1=40 używa 3 epok od 10% bazowego LR i zachowuje dodatkowy
  milestone `e3=20`; historyczne konfiguracje domyślnie zachowują warm-up 0.
- `[chat:validation]` Compileall i pięć czysto logicznych regresji lokalnych
  przeszły. Compute test schedulera i stoppera wysłano jako `20257431`.
- `[chat:warmup-gate]` Pierwszy compute gate wykrył, że `SequentialLR`
  przełączał scheduler przed osiągnięciem bazowego LR (0,42 zamiast 0,6).
  Kompozycję poprawiono na `ChainedScheduler`, aby warm-up i bazowy
  MultiplicativeLR wykonywały się w prawidłowej kolejności.
- `[chat:warmup-gate-r2]` Drugi gate potwierdził dojście LR 0,06→0,6;
  niepowodzenie dotyczyło wyłącznie błędnej asercji testu oczekującej
  `last_epoch` na kontenerze `ChainedScheduler`. Resume jest sprawdzane przez
  pełny `state_dict` schedulerów składowych.
- `[chat:launch]` Finalny test schedulera wysłano jako `20257602` (10 min),
  a zależny czterofazowy smoke jako `20257606` (15 min).
- `[chat:final-gates]` `20257602` przeszedł w 1:11. Czterofazowy smoke
  `20257606` przeszedł w 1:54, wykonał warm-up, zamroził hipotetyczny
  `recovery_plateau` w `e3=3`, wybrał `best_feasible` zamiast rollbacku i
  poprawnie uruchomił Phase 4 z wybranego checkpointu.
- `[chat:final-static]` `git diff --check`, pełny czysto logiczny
  `tests/test_validation_control.py` i `tests/test_repository_contracts.py`
  zakończyły się kodem 0 w lokalnym środowisku CPU.
- `[chat:full-launch]` Nowe pełne P1=40 observe-only wysłano z limitem 2 h:
  `20257703` seed 83, `20257705` seed 184 i `20257704` seed 285. Runy używają
  accuracy-first, 3-epokowego warm-upu i milestone `e3=20,40,60,80,200`.
- `[chat:per-step-warmup]` Na decyzję użytkownika anulowano `20257703`,
  `20257704` i `20257705`. Warm-up Phase 3 zmieniono na 4 epoki od LR/10,
  z liniowym wzrostem po każdym kroku optymalizatora i pełnym stanem resume.
- `[chat:per-step-gate]` Statyczna kompilacja, repo contracts i
  `git diff --check` przeszły. Compute test `20257994` zakończył się
  `COMPLETED (0:0)` (2 testy); czterofazowy smoke online wysłano jako
  `20258142` z limitem 15 min.
- `[chat:per-step-only]` Usunięto wywołanie schedulera epokowego z Phase 3;
  po czteroepokowym warm-upie per-step LR pozostaje stałe. Compute gate
  `20258248` przeszedł 3/3 testy, a finalny smoke `20258291` zakończył się
  `COMPLETED (0:0)` w 2:00.
- `[chat:p3-relaunch]` Ponownie wysłano pełne P1=40 stopper observe/replay:
  `20258725` seed 83, `20258727` seed 184 i `20258726` seed 285; każdy
  ma limit 2 h, P4=200 i logowanie online do projektu Phase3Stopping.
